<ol class="post-list">
    <li>
        <article class="post">
            <div class="title"> <a href="{{post_url}}" target="blank">{{title}}</a></div>
            <div class="meta">
                {{user.username}} on {{created_at}}
                <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
            </div>
        </article>
    </li>
</ol>